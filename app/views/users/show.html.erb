Lists for <b><%= @user.name %></b>:
<select>
    <%= @user.lists do |list, i| %>
        <option value=<%= i %>><link_to list.title, list_path(list)></option>
    <% end %>
</select>

<p>Create List: <%= link_to "New List", new_user_list_path(@user) %> </p>